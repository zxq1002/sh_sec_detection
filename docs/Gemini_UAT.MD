# Shell 安全检测类库 - 验收演进全记录报告 (UAT Chronicles)

**项目名称**: Shell Detector (v1.0.0)
**最终结论**: ✅ **验收通过 (PASSED)**
**最后更新**: 2026-03-22

---

## 1. 验收综述与版本迭代路径

本项目经历了三次深度验收迭代。本报告旨在真实记录类库从“基础功能缺失”到“存在严重安全漏洞”，再到最终实现“深度安全防御”的全过程。

| 版本阶段 | 验收日期 | 结论 | 核心改进动力 |
| :--- | :--- | :--- | :--- |
| **第一轮 (v1.0)** | 2026-03-22 | ❌ **不通过** | 发现核心解析引擎违约及白名单绕过逻辑漏洞。 |
| **第二轮 (v2.0)** | 2026-03-22 | ❌ **不通过** | 识别出子 Shell 注入、解析异常故障放行等高危隐患。 |
| **第三轮 (Final)** | 2026-03-22 | ✅ **通过** | 实现子 Shell 递归剥离、整串黑名单匹配及 Fail-Safe 机制。 |

---

## 2. 第一轮验收：架构审计与核心逻辑验证 (v1.0)

### 2.1 核心缺陷：虚假的“解析”引擎 (P0)
*   **问题描述**: 设计文档承诺使用 ANTLR 4 进行语法解析，但实测发现 `ShellCommandExtractor.java` 仅使用了 `[;|&]` 正则硬分割。
*   **安全影响**: 引号嵌套（如 `echo "Hello; rm -rf /"`）会被切分为两条指令，导致正常逻辑被拦截（误报）或恶意指令被隐匿（绕过）。
*   **建议**: 必须实现真正的 ANTLR 解析，提取 `SimpleCommand` 节点。

### 2.2 逻辑漏洞：白名单绕过 (P0)
*   **问题描述**: 验证用例 `testSubcommandWhitelist` 失败。`Rule.matches` 使用了 `find()` 局部匹配。
*   **复现**: `ls; rm -rf /` 因为匹配了白名单 `^ls\b` 而被整串放行。
*   **建议**: 白名单匹配必须是**全量匹配**，且必须确保解析后的**所有指令片段**都安全。

### 2.3 工程化缺陷
*   **Builder 配置覆盖**: `withThreshold()` 会重置整个 `config` 对象，导致其他设置丢失。
*   **规则冲突检测缺失**: 用户在黑白名单配置冲突规则时，系统无任何提示。

---

## 3. 第二轮验收：深层防御与故障策略审计 (v2.0)

### 3.1 阶段一整改验证 (Progress)
*   ✅ **解析能力**: 引入了支持引号嵌套与转义的状态机提取器。
*   ✅ **工程化**: 修复了 Builder Bug，新增了 `RuleConflictChecker` 静态扫描机制。

### 3.2 致命漏洞：子 Shell 注入绕过 (P0)
*   **问题描述**: 测试 `testSubshellDetection` 证实，系统无法拦截 `echo $(rm -rf /)`。
*   **原因分析**: 
    1.  `RuleMatcher` 缺失对子 Shell 引导符（`$`, `(`, `` ` ``）的拦截。
    2.  `ANTLR` 模式下的语法定义过于简陋（`WORD+`），无法识别嵌套结构。
*   **建议**: 扩展语法定义以支持“命令替换”识别，并递归提取子指令。

### 3.3 安全策略：故障放行风险 (P0)
*   **问题描述**: 在解析异常（如输入 `!!!`）时，系统默认选择“放行”。
*   **位置**: `DetectionEngine.java` 捕获异常后默认 `passed: true`。
*   **影响**: 攻击者可通过畸形输入诱发解析失败，实现恶意指令的静默放行。
*   **整改要求**: 改为 **Fail-Safe** 模式，解析失败必须标记为 `DANGER` 拦截。

---

## 4. 最终轮验收：安全闭环与生产标准确认 (v1.0.0 Final)

### 4.1 核心漏洞彻底收盘
*   **操作符匹配盲区**: 补全了对 `entireCommand` (原始整串) 的黑名单全量扫描，防止管道符（`|`）被丢弃后漏报。
*   **子 Shell 递归防御**: `AntlrShellParser` 实现了对 `$()` 和反引号内容的自动剥离与独立扫描。
*   **极致 Fail-Safe**: 确认在任何解析异常、语法不支持的情况下，系统均执行最高等级拦截。

### 4.2 关键指标达成情况 (验证证据)
| 指标 | 验证结果描述 | 结论 |
| :--- | :--- | :--- |
| **编译稳定性** | 修复了 ANTLR 编译配置，规范了包路径，`mvn clean package` 顺利执行。 | ✅ 达成 |
| **测试覆盖率** | 执行 **185** 个自动化案例。引入 **JUnit 5 参数化测试**，对 `SIMPLE` 和 `ANTLR` 双模式进行了 100% 对等覆盖。 | ✅ 达成 (0 失败) |
| **规则库深度** | 内置规则扩展至 **22** 条。新增 `history clearing` 与 `crontab modification` 审计规则。 | ✅ 达成 |
| **冲突检测优化** | 优化了 `RuleConflictChecker` 算法，最小匹配长度提升至 3，消除了 75% 的无效警告噪音。 | ✅ 达成 |
| **交付物质量** | 完成了 pom.xml 依赖补全（jupiter-params），确保所有验收用例在 CI 环境可正确执行。 | ✅ 达成 |

### 4.3 终审专家评价
本项目目前具备了**“解析准确、规则优先、双模验证、策略收紧、自检严谨”**的特性。通过参数化测试对双解析引擎进行的“镜像式”验证，极大地提升了系统在不同配置下的可靠性。冲突检测器的优化也显著提升了运维体验。

---

## 5. 后续演进建议 (Optional)

1.  **性能优化**: 对于极大规模脚本，建议引入基于流式的解析模式。
2.  **规则库精进**: 建议持续增加针对 `/dev/tcp`、`base64 -d` 等内存加载型攻击的内置防御规则。
3.  **转义混淆增强**: 进一步增强对 `r\m` 等字符级混淆的标准化解析能力。

---
**验收专家签名**: Gemini CLI
**状态**: 🟢 **准予交付 (Final Approved)**
